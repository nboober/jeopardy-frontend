import React from 'react';

let Column = (props) => {
    console.log(props.cat1.map(c => c.answer))
    return (
        <div>
        <h3>columns
        </h3>
    
        </div>
    )
}

export default Column